# Keep the build context small and the image free of local state.
.venv
.git
.gitignore
__pycache__
*.pyc
.pytest_cache
.ruff_cache
.scratch

# Runtime state and output belong on the mounted volume, never in the image.
seen.db
digest.md

# Config comes from the mounted share so it can change without a rebuild.
config.yaml
